# GAT Training Configuration
# Complete configuration for training GAT model on MovieLens

defaults:
  - data: movielens
  - model: gat
  - trainer: gat_trainer

# Override defaults if needed
# data:
#   env: prod  # Use larger dataset

# model:
#   hidden_dim: 256  # Larger embeddings
#   num_heads: 4     # More attention heads

# trainer:
#   num_epochs: 200  # Longer training

