# GAT Model Configuration
# Graph Attention Network for user-item recommendation

name: GAT
module: "recommender.models.gat:GAT"
params:
  in_dims:
    user: 100 # Overridden by data
    item: 100 # Overridden by data
  metadata: null # Overridden by data
  hidden_dim: 128
  num_heads: 2
  num_layers: 2
  dropout: 0.1

