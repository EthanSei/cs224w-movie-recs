# Two-tower Model Configuration
# Two-tower model for user-item recommendation

name: TwoTower
module: recommender.models.two_tower:TwoTower
params:
  in_dims: null  # Will be set from data dimensions in train.py
  hidden_dim: 256
  num_layers: 2
  dropout: 0.01